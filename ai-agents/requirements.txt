# Multi-agent orchestration framework
crewai==0.35.0

# For wrapping Gemini LLM and embedding models
langchain-google-genai==0.0.11

# Vector database for local text chunk persistence and retrieval
chromadb==0.4.24

# High-performance PDF text parsing and layout extraction
pymupdf==1.24.5

# For building backend API integration endpoints
fastapi==0.111.0
uvicorn==0.30.1

# For declaring strict type schemas for structured agent outputs
pydantic==2.7.4

# For safely reading local environment credentials and API keys
python-dotenv==1.0.1

# For async HTTP webhook callbacks to the FastAPI backend
httpx>=0.27.0
